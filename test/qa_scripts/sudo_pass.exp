#!/usr/bin/expect -f
set timeout 20
set password "101abn"

spawn sudo apt-get install -y expect jq
expect "*?assword:*"
send -- "$password\r"
expect eof

spawn chmod +x verify_vm_access.exp
expect eof

spawn ./verify_vm_access.exp
expect eof
